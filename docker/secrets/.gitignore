# Nothing in here is ever committed. This directory is the default mount point
# for the workflows service's Gmail credential; the real one usually lives
# outside the repo and is pointed at with GMAIL_SECRETS_DIR.
*
!.gitignore
